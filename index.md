- [[project]] — what is Magpie, how is it structured, where does pasteboard/storage/hotkey/dictation code live, what does the screen-capture branch add
- [[decisions]] — why no Xcode project, why a self-signed cert instead of ad-hoc signing, why /tmp build path, why on-device batch dictation
- [[gotchas]] — Accessibility prompt keeps coming back, hotkey silently not firing, swift build sqlite I/O error, Screen Recording needs relaunch, own pasteboard writes re-ingested
- [[procedures]] — how to build/run Magpie, bundle.sh, one-time codesign cert setup, which permissions to grant, regenerate emoji data
- [[lessons]] — how to add a new panel feature or a new macOS permission the Magpie way, how to verify without tests

## Attention

This brain is newborn and thin. Capture knowledge in journal/ as you work; grooming moves it here.
