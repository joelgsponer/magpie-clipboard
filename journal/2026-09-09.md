## s=87a1b05a — brain init, merged origin/screen-capture into main, release build interrupted
- [procedure] `git pull` on main was already up to date; `origin/screen-capture` (d855d0e) was merged as a fast-forward, main now includes ⌘⇧X screen capture via `claude -p`.
- [gotcha] A cold release build (`./bundle.sh`) exceeded 10 minutes compiling FluidAudio whole-module with `-O` and got killed with the session; `Magpie.app` was left at the pre-merge binary (dated 25 Aug). Use `./bundle.sh debug` for iteration, or run the release build with `run_in_background` and a long timeout.
- [fact] `bundle.sh` only `rm -rf`s `Magpie.app` after `swift build` succeeds, so an interrupted build leaves the previous app intact and still signed with "Magpie Local Codesign".
- [fact] Verified on this machine: `security find-identity -v -p codesigning` lists "Magpie Local Codesign" (33F84681…), and `codesign -dvv Magpie.app` shows `Authority=Magpie Local Codesign`. Permissions will persist across rebuilds; the only new one-time grant after the merge is Screen Recording, which needs a relaunch.
- [fact] User TCC db (`~/Library/Application Support/com.apple.TCC/TCC.db`) shows Microphone and SpeechRecognition granted for `local.magpie`; Accessibility and ScreenCapture live in the system TCC db and were not checked.
- [fact] `Sources/Magpie/Permissions/ScreenRecording.swift` documents: no Info.plist key exists for Screen Recording; `CGPreflightScreenCaptureAccess()` caches for process lifetime; spawning `/usr/sbin/screencapture` still checks TCC against Magpie.
- [decision] User intent (pending): after the merge and install, they want to "reframe the whole app concept" — a new direction not yet described. Next session should ask what the reframe is before building.
- [procedure] kb-lint must run from the project root (`kb-lint.sh .engram/mind`), not from inside the mind worktree, or every `Sources/...` citation reports as stale.
